## IMPORTANTE
- Crear repositorio en github
- Ponerlo público. En caso de hacerlo privado, agregar a la cuenta "brunoricciweb" como collaborator para que el docente pueda tener acceso.
- para el ejercicio de la aerolínea, crear una carpeta y en su interior colocar las queries correspondientes a cada uno de los ejercicios, en 
archivos con extensión .sql .
- En caso de poner todas las queries en el mismo archivo, poner un comentario con el ejercicio al que corresponde cada una.
